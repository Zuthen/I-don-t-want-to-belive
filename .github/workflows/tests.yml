name: AUTOMATED MULTIPLAYER DEPLOY

on:
  push:
    tags:
      - 'v*'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Generate Version File
        run: echo "${{ github.ref_name }}" > "i-(don't)-want-to-belive/version.txt"

      - name: Setup Godot (Standard Version with Templates)
        uses: chickensoft-games/setup-godot@v2
        with:
          version: 4.6.2
          use-mono: false
          include-templates: true
          download-butler: false # Blokuje błędy i odpytywanie starej domeny itch.ovh

      - name: Import project
        working-directory: i-(don't)-want-to-belive
        run: |
          godot --headless --import

      - name: Run tests
        working-directory: i-(don't)-want-to-belive
        run: |
          godot --headless -s res://addons/gut/gut_cmdln.gd -gdir=res://tests -gprefix=test_ -gexit

      - name: Export Project for Windows
        working-directory: i-(don't)-want-to-belive
        run: |
          mkdir -p ../build
          godot --headless --export-release "Windows Desktop" ../build/game.exe

      - name: Deploy to itch.io via Direct Butler
        env:
          BUTLER_API_KEY: ${{ secrets.ITCH_IO_API_KEY }}
        run: |
          # Pobieramy bezpośrednio skompilowany plik binarny 'butler' dla Linuksa (bez ZIP, bez rozpakowywania)
          curl -L -o butler https://itch.zone
          chmod +x butler
          
          # Wypychanie buildu gry na serwer itch.io
          ./butler push ../build Zuthen/nie-chc-uwierzy:windows --userversion ${{ github.ref_name }}
